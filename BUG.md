# Coin Memo 缺陷与产品质量记录

> 本文件记录影响用户数据可信度、导出完整性或核心交互的缺陷，以及对应的根因、方案取舍和回归要求。
>
> 产品原则：导出功能只能生成完整、可验证的文件；无法保证完整性时必须明确失败，不能把内容截断、遗漏或生成表面成功的残缺文件交给用户。

## 2026-08-04：PDF 跨页截断与导出架构重做（1.0.23）

### 问题与影响

旧实现先用 `html2canvas` 把全部账单渲染为一张超长 Canvas，再由 `jsPDF` 按固定 A4 高度裁切图片。裁切位置只认识像素坐标，不理解日期分组、表格行等内容边界，因此会把日期卡片或记录从中间切开。

这不是单纯的视觉瑕疵：PDF 是用户保存、归档和核对账目的交付物。内容在页边界被切断会降低账单可信度，严重时可能让用户误以为记录缺失。产品级插件不应继续提供已知会生成不完整结果的导出交互。

### 根因

- `html2canvas` 负责把 DOM 栅格化，不提供 PDF 分页语义，也不识别 `break-inside` 等打印分页规则。
- 对一张长图按 A4 像素高度切片时，分页边界与日期分组、表格行完全无关。
- 超长 Canvas 在移动端还存在尺寸和内存限制，数据量增大后可能出现空白、截断或渲染失败。
- 旧代码同时维护预览 DOM 和一套字符串拼接的 PDF HTML，结构重复，容易产生样式和内容差异。

### 方案探索与回滚

1. **尝试使用 jsPDF 原生绘制。** 该方案能在绘制阶段计算分页，并生成可选择、可搜索的矢量文本。
2. **发现中文字体约束。** jsPDF 内置字体不包含中文字形；要稳定输出中文必须额外加载并嵌入字体文件，会增加插件体积、启动或导出成本，也引入字体授权和跨平台一致性问题。
3. **回滚原生绘制方案。** 在不接受额外字体库的前提下，继续使用浏览器已经能够正确显示中文的 DOM，再由 `html2canvas` 栅格化。
4. **排除其他方案。** Electron `printToPDF` 仅适合桌面端；系统打印在移动端需要额外交互且行为不一致；html2pdf.js 本质上仍依赖长 Canvas，无法从根本上保证语义边界。

### 最终修复

最终采用“先语义分页，再逐页截图”的架构：

- 在 DOM 阶段生成固定 `720 x 1018` CSS 像素的 A4 页面，而不是生成一张超长 Canvas。
- 标题、时间范围和统计概览作为整体内容块放置。
- 分类统计优先整体换页；超过一页时只在表格行之间拆分，并在续页重复分类标题和表头。
- 日期分组优先整体换页，不再允许页边界穿过普通日期卡片。
- 单日记录确实超过一页时，只在记录行之间拆分，并在下一页重复日期头，保证单条记录不被切开。
- 每个固定页面单独调用 `html2canvas`，使用 2 倍缩放生成约 2.93MP 的 Canvas，控制在移动端常见 Canvas 限制以内。
- `jsPDF` 只负责把每页 Canvas 组装成 A4 PDF 并下载，不绘制中文，因此不需要字体库。
- 导出前检查每一页是否溢出，并核对分页后的记录数量是否与源记录一致。
- 如果分类行或单条记录大到一页都无法容纳，终止导出并提示失败，不生成内容不完整的 PDF。
- 每页写入 PDF 后立即释放 Canvas，降低连续导出多页时的内存峰值。

### 同步优化

- 删除旧的 `generatePDFHTML()`、`DOMParser` 临时文档、长图切片变量和 `.pdf-temp-container` 样式。
- 预览与导出复用同一套 DOM 生成逻辑，减少内容不一致风险。
- 导出页面固定使用浅色变量和桌面三列统计布局，不受当前主题或移动端媒体查询影响。
- PDF 续页不再显示无必要的“详细记录（续）”标题，直接展示日期分组。
- Markdown 和 PDF 共用文件名规则：完整自然月使用 `每日记账_YYYY-MM`，非整月保留起止日期。

### 方案取舍

- PDF 页面仍然是栅格图片，文字不可选择或搜索。
- 这是“不加载中文字体库、保持桌面与移动端兼容、保证分页完整性”条件下的明确取舍。
- 如果未来重新要求矢量文本，必须先确定可接受的中文字体来源、授权、体积和按需加载策略，不能直接恢复缺少中文字形的原生绘制。

### 验证与回归要求

- `npm run lint` 必须零错误、零警告。
- `npm run build` 必须成功并生成完整 release 资源。
- 验证普通日期组靠近页尾时会整体移到下一页。
- 验证单日大量记录跨多页时，只在记录行之间分页并重复日期头。
- 验证分类数量跨页、空描述、长中英文描述和金额列均完整显示。
- 验证完整自然月与非整月的 Markdown/PDF 文件名。
- 任何分页异常都必须使导出失败，禁止移除页溢出检查和记录数量校验来“绕过”错误。

### 相关历史

- `a6fcd6a`：尝试改用 jsPDF 原生绘制。
- `21886ce`：因中文字体依赖问题回滚到 html2canvas。
- `1.0.23`：采用语义分页和逐页栅格化完成最终修复。
## 2026-08-10：统计视图时间下拉被统计卡片遮挡（1.0.24）

### 问题与影响

在「记账统计」视图展开「时间」筛选下拉后，菜单只露出第一项，其余选项被下方的统计卡片（总收入/总支出/结余/预算状态）盖住。当预算超支、`budget-alerts` 出现在统计卡片之前时，告警条同样会盖住菜单。

用户实际上无法看到并选中大部分时间范围选项，核心筛选交互不可用。主视图头部的时间/分类/更多下拉存在同样的结构风险。

### 根因

层叠上下文（stacking context）问题，与元素尺寸和 `overflow` 无关。

- `.filter-dropdown-menu` 是 `position: absolute` + `z-index: 100`，但它的定位祖先 `.filter-dropdown` 位于 `.accounting-filters` 内部。
- 暗色主题样式给 `.accounting-filters` 加了 `backdrop-filter: blur(...)`，该属性会创建新的层叠上下文，于是菜单的 `z-index: 100` 只在 `.accounting-filters` 内部生效，无法与外部兄弟节点比较层级。
- `.stat-card` 和 `.budget-alert` 在暗色主题下同样带 `backdrop-filter`，各自创建层叠上下文。
- `.accounting-filters` 与 `.accounting-stats` 都是 `z-index: auto`，同级时按 DOM 顺序绘制；统计区在后，因此整体覆盖筛选区子树，包括已展开的下拉菜单。
- 浅色主题没有 `backdrop-filter`，菜单能正常浮在上层，所以该缺陷只在暗色主题下暴露。

### 最终修复

在 `styles.css` 中显式声明三层层级，不再依赖 `z-index: auto` 的 DOM 顺序：

- `.accounting-header`：`position: relative; z-index: 30;`（主视图头部下拉）
- `.accounting-filters`：`position: relative; z-index: 20;`（统计视图筛选区）
- `.accounting-stats`、`.main-page-stats`、`.accounting-records`：`position: relative; z-index: 0;`

统计区被显式压到 `z-index: 0` 后自身成为层叠上下文，`.stat-card:hover` 的 `transform` 也无法再冒到筛选区之上。

### 方案取舍

- 保留统计卡片和预算告警的 `backdrop-filter` 毛玻璃效果，只调整层级，不为了修复遮挡而牺牲既有视觉设计。
- 未改用 `document.body` 挂载浮层或 Obsidian `Menu` 重写下拉，纯 CSS 即可解决，避免引入定位计算和滚动跟随的额外复杂度。
- 下拉菜单背景使用不透明的 `var(--background-primary)`，展开后能完整遮住下方内容，无需额外遮罩。

### 验证与回归要求

- `npm run lint` 与 `npm run build` 必须通过，且 `dist/styles.css` 包含新增层级规则。
- 暗色主题下打开统计视图，展开时间下拉，五个选项必须全部可见可点击。
- 在预算超支、页面出现预算告警条时重复上述验证。
- 主视图头部的时间、分类、更多三个下拉在暗色主题下都不能被下方统计卡片或记录列表遮挡。
- 浅色主题需回归确认层级调整未引入新的遮挡或阴影异常。
- 后续如果给筛选区、头部或统计区新增 `backdrop-filter`、`filter`、`transform`、`opacity` 等会创建层叠上下文的属性，必须重新验证浮层层级。
